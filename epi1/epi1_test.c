#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#if 0
    // 编写一个 标准输出hello world!
    printf("hello world!\n");
#endif

int main(void)
{
    // 编写一个程序，从标准输入读取几行输入，每行输入都要打印在标准输出上，前面要加上行号
    // 试图让程序能够处理的输入行的长度没有限制(动态数组分配空间)
    // scanf("%s",);
    return 0;
}